#!/bin/sh

hostname $(cat /etc/hostname)

# horrible idea
. /etc/os-release

echo "\n --- \e[94m$NAME $VERSION_ID ($(uname -sr))\e[0m --- "

setsid respawn getty /dev/tty1 &
setsid respawn getty /dev/tty2 &
setsid respawn getty /dev/tty3 &
setsid respawn getty /dev/tty4 &